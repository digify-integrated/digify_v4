<?php

declare(strict_types=1);

use App\Core\Config;

return [

    // ================================================================
    // Application Identity
    // ================================================================
    'name' => Config::string('APP_NAME', 'Digify'),

    // ================================================================
    // Environment
    // Values: local | staging | production
    // ================================================================
    'env' => Config::enum('APP_ENV', ['local', 'staging', 'production'], 'local'),

    // ================================================================
    // Debug Mode
    // ================================================================
    'debug' => Config::bool('APP_DEBUG', false),

    // ================================================================
    // Base URL
    // ================================================================
    'url' => Config::url('APP_URL', 'http://localhost'),

    // ================================================================
    // Timezone
    // ================================================================
    'timezone' => Config::string('APP_TIMEZONE', 'Asia/Manila'),

    // ================================================================
    // Localization
    // ================================================================
    'locale' => Config::string('APP_LOCALE', 'en'),
    'fallback_locale' => Config::string('APP_FALLBACK_LOCALE', 'en'),

    // ================================================================
    // Security Key
    // ================================================================
    // Validated as: non-empty, 32-byte base64
    'key' => Config::secureKey('APP_KEY'),

];
